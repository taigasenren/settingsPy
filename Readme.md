Run this code in the directory you want to zip.
When git clone finshes, please press enter button.
And then zip file will be completed.

```
git clone git@github.com:taigasenren/settingsPy.git
cd settingsPy
python settings.py
cd ..
rm -rf settingsPy
```

